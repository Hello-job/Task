export * from './user';
export * from './project';
/**
 * 文件上传接口
 */
const upload = '/upload';

/**
 * 创建会话id
 */
const createSessionId = '/createSessionId';

/**
 * 创建会话记录
 */
const getSessionRecord = '/getSessionRecord';

export { upload, createSessionId, getSessionRecord };
