/**
 * 注册接口
 */
const register = '/register';

/**
 * 登陆接口
 */
const login = '/login';

/**
 * 更新用户信息
 */
const updateUserInfo = '/updateUserInfo';

/**
 * 获取用户信息
 */
const getUserInfo = '/userinfo';

/**
 * 获取全部用户
 */
const getUserAll = '/getUserAll';

export { register, updateUserInfo, getUserInfo, getUserAll, login };
